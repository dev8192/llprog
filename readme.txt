llprog is a set of exercises on Windows programming with fasm.

Why fasm?
Because it is the only assembler with the following properties:
-- no dependencies
-- self-compiling (written in its own language)
-- has its own linker
-- production grade

It's all you need to start writing cool programs for Windows (including GUI programs).
